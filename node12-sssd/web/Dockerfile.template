{%- import 'macros.jinja2' as macros -%}
# Toolforge NodeJS 12 webserver image
# Name: docker-registry.tools.wmflabs.org/toolforge-node12-sssd-web
FROM {{registry}}/{{image_prefix}}-node12-sssd-base:{{tag}}

# Install Python3 for backwards-compatibility. Do not
# copy forward to new images
{{ macros.apt_install(
    [
        "python3",
    ]
) }}
COPY webservice-runner /usr/bin/
{# vim:sw=4:ts=4:sts=4:et:ft=django: #}
