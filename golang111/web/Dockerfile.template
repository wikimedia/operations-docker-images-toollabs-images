# Toolforge Golang webserver image
# Name: docker-registry.tools.wmflabs.org/toolforge-golang111-web
FROM {registry}/{image_prefix}-golang111-base:{tag}

RUN apt-get update \
    && DEBIAN_FRONTEND=noninteractive \
    apt-get install --yes -t buster-backports \
    toollabs-webservice \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*
